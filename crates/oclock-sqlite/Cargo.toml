[package]
edition = "2024"
name = "oclock_sqlite"
version = "0.1.5"

authors = ["Davide Ghilardi <dghila@d71.dev>"]
description = "Time tracking utility (sqlite component)"
keywords = [ "time-tracking", "daemon", "utility" ]
repository = "https://github.com/dghilardi/oclock"
license = "GPL-3.0-or-later"
readme = "../../README.md"
# Note: package name kept as oclock_sqlite for crates.io compatibility

[dependencies]
log = "0.4"

diesel = { version = "2.3", features = ["sqlite"] }
diesel_migrations = { version = "2.3", features = ["sqlite"] }

serde = { version ="1.0", features = ["derive"] }
